#pragma once
//=================================================================
//Possible FSM States
//=================================================================

enum FSMState {
	FSM_NULL = 0,
	FSM_ATTRACT,
	FSM_PLAY,
	FSM_PAUSE,
	FSM_GAME_OVER,
	FSM_COUNT
};

